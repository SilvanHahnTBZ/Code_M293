import { XMarkIcon } from '@heroicons/react/24/solid';

const ExitButton = () => {
  return (
    <button>
      <XMarkIcon className="text-theme-50 h-9 w-9"/>
    </button>
  );
};

export default ExitButton;
